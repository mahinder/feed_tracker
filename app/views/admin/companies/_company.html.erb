<li style="padding: 5px">
  <% semantic_form_remote_for company, :url => admin_company_path(company), :method => :put do |f| %>
      <%= f.text_field :name %>
      <%= f.submit('Update') %>
  <% end %>
  <%= link_to_remote "Destroy", :url => admin_company_path(:id => company), :method => :delete, :confirm => "Are you sure?" %>
</li>